# Rollback — 1.0.1 (Spec 01: Sistema de diseño / theming)

**Fecha:** 2026-09-15
**Tag:** `1.0.1`
**Commit fusionado a `master`:** `cd09795cd79a038eb31fef4c762145311e151cb1`
**Merge:** `spec-01-design-system-theming` → `master` (fast-forward, sin commit de merge separado)

## Qué se fusionó

Sistema de theming extensible (colores primario/secundario/terciario + fonts) con 3 themes
de ejemplo (`default`, `corporate`, `vibrant`) seleccionables vía `EXPO_PUBLIC_THEME`:

- `src/constants/themes/{types,default,corporate,vibrant,index}.ts` (nuevos)
- `src/constants/theme.ts` (actualizado como fachada, re-exporta `Colors`/`ThemeFonts`)
- `src/components/themed-text.tsx` (aplica `ThemeFonts.heading`/`body` según variante)
- `.env.example` (documenta `EXPO_PUBLIC_THEME`)
- `package.json`, `package-lock.json`, `eslint.config.js` (setup de ESLint, requerido para
  validar el criterio de aceptación de lint — el repo no tenía config previa)

## Cómo revertir (opción recomendada — no destructiva)

```bash
git revert -m 1 cd09795cd79a038eb31fef4c762145311e151cb1
```

> Nota: como el merge fue fast-forward, no hay un segundo padre real; si `-m 1` falla porque
> el commit no es un merge commit, usar simplemente:
>
> ```bash
> git revert cd09795cd79a038eb31fef4c762145311e151cb1
> ```

Esto crea un commit nuevo que deshace los cambios de esta spec sin reescribir historia,
seguro para usar aunque `master` ya haya sido pusheado.

## Alternativa destructiva (solo si `master` NO fue pusheado a ningún remoto)

```bash
git reset --hard 40e9dba
```

⚠️ **Advertencia:** esto reescribe el historial local de `master` y descarta el commit
`cd09795` por completo. No usar si la rama ya fue compartida/pusheada — usar `git revert`
en ese caso.

## Tag estable anterior

No existe un tag previo a `1.0.1` (esta es la primera spec de este repo implementada con
este flujo). El commit previo en `master` era `40e9dba` ("add first sepec").
