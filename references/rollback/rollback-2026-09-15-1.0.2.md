# Rollback — 1.0.2 (Spec 02: Pantalla de Dashboard)

**Fecha:** 2026-09-15
**Tag:** `1.0.2`
**Commit fusionado a `master`:** `3dfa649c35d4ec61825cc517545f4c4d83df19c9`
**Merge:** `spec-02-dashboard-screen` → `master` (fast-forward, sin commit de merge separado)

## Qué se fusionó

Nueva pantalla "Dashboard" (primer tab de la app) con header de usuario, stat cards y lista
de actividad reciente, con datos mock y un placeholder de autenticación, sobre el sistema
de theming de la spec 01:

- `src/hooks/use-auth.ts` (nuevo): `useAuth()` mock fijo
- `src/hooks/use-dashboard-data.ts` (nuevo): `useDashboardData()` con datos mock y delay simulado (~800ms)
- `src/components/dashboard/{dashboard-header,stat-card,activity-list}.tsx` + sus skeletons (nuevos)
- `src/app/dashboard.tsx` (nuevo): compone la pantalla completa
- `src/components/app-tabs.tsx`, `src/components/app-tabs.web.tsx` (actualizados): registran
  "dashboard" como primer tab, antes de `index`/`explore`
- `specs/02-dashboard-screen.md` (actualizado): Estado → Implemented

## Cómo revertir (opción recomendada — no destructiva)

```bash
git revert -m 1 3dfa649c35d4ec61825cc517545f4c4d83df19c9
```

> Nota: como el merge fue fast-forward, no hay un segundo padre real; si `-m 1` falla porque
> el commit no es un merge commit, usar simplemente:
>
> ```bash
> git revert 3dfa649c35d4ec61825cc517545f4c4d83df19c9
> ```

Esto crea un commit nuevo que deshace los cambios de esta spec sin reescribir historia,
seguro para usar aunque `master` ya haya sido pusheado.

## Alternativa destructiva (solo si `master` NO fue pusheado a ningún remoto)

```bash
git reset --hard 4bcdc42
```

⚠️ **Advertencia:** esto reescribe el historial local de `master` y descarta el commit
`3dfa649` por completo. No usar si la rama ya fue compartida/pusheada — usar `git revert`
en ese caso.

## Tag estable anterior

`1.0.1` (spec 01 — sistema de theming), commit `cd09795cd79a038eb31fef4c762145311e151cb1`.
